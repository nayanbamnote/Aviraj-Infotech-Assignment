export type ProjectType = {
    title: string
    description: string
    link: string
}

export type WhyChooseUsArrayType = {
    quote: string
  name: string
  title: string
}

export type MemberArrayType = {
    name: string;
    imageUrl: string; 
    description: string;
    role: string;
}